## 프로그래머스 42577. 전화번호 목록

### 문제 설명

- 전화번호부에 적힌 전화번호 String 배열이 주어짐
- 어떤 한 번호가 다른 번호의 접두어라면 false, 아니면 true 반환

### 문제 해결 방법

- 초기 테스트 예제 풀이를 위해 for문 중첩으로 문제 해결
- 그러나 `O(n^2)` 시간이 걸리므로 시간 초과 오류 발생
- 이를 위해 `Arrays.sort()` 메서드를 사용해서 배열을 정렬
- 정렬 이후 i번째와 i+1번째 문자열만 비교하였고, 이는 `O(n)` 만큼의 시간 소요
