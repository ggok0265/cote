## 백준 11004. K번째 수

### 문제 설명

- n과 k, 그리고 n개의 수가 주어짐
- n개의 수를 오름차순 정렬 후 앞에서부터 k번째 수 찾기

### 문제 해결 방법

- 시간 초과 방지를 위해 `BufferedReader`와 `StringTokenizer` 사용
- 빠른 정렬을 위해 받아온 원소를 `ArrayList`에 저장
- k의 인덱스는 1부터 시작하기 때문에, 정렬된 `ArrayList`의 k-1번째 값을 정답으로 반환
